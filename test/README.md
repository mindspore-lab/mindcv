- `modules` for unit test (ST): test all modules. Currently, test_optim is implemented. 

- `tasks` for system test (UT): test the train/finetune/infer pipeline for cnn/transformer on different kind of datasets
